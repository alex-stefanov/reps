# UX Fix Log

One line per finding resolved from `docs/ux-audit.md`.

UX-001 — Prefill scanned amount from cents (`(cents/100).toFixed(2)`) instead of round-tripping through `formatEuros`; hardened `parseEuros` to strip thousands separators; added round-trip + grouped-amount unit tests — `components/add-finance-form.tsx`, `lib/core/finance.ts`, `tests/unit/finance.test.ts` — verified ✅ (16 finance unit tests pass; typecheck/lint clean on changed files)
UX-002 — Added route error boundary (retry), global-error boundary, and styled 404, all in the Clay & Glass voice with the character portraits — `app/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx` — verified ✅ (dev server: unknown route renders the styled 404, a thrown page renders the recoverable error state with a working Try-again; lint + full typecheck clean)
UX-003 — Capped the Home-path GitHub fetch with `AbortSignal.timeout(4s)`; expiry lands in the existing "unavailable" branch (pending, never faked). Added a deterministic hung-connection unit test — `lib/core/github-verify.ts`, `tests/unit/github-verify.test.ts` — verified ✅ (78 unit tests pass incl. the new hung-fetch case; typecheck + lint clean)
UX-007 — Added a first-run Finance empty state (CoinIcon, "Start your ledger", "Add your first entry" CTA → /finance/add) shown when the account has no entries, replacing empty charts/Sankey — `components/finance-screen.tsx`; updated the onboarding e2e to expect it — verified ✅ (6/6 finance e2e pass incl. the empty-state assertions; lint + typecheck clean)
UX-008 — Added route-level loading skeletons for /finance, /schedule, /ideas, /tutorials (shared Shimmer/HubHeaderSkeleton), each mirroring its screen's card layout; pulse is neutralized under prefers-reduced-motion — `components/skeletons.tsx`, `app/{finance,schedule,ideas,tutorials}/loading.tsx` — verified ✅ (rendered a skeleton at runtime and screenshotted the matched layout; lint + typecheck clean)
UX-004 — Honor OS Reduce Motion: global @media (prefers-reduced-motion) rule neutralizes CSS keyframe loops/transitions (pulse dot, refuse shake, skeleton pulse); <MotionConfig reducedMotion="user"> makes framer-motion cross-fade instead of spring; gated the character's breathing loop, pointer parallax, and idle fidget behind useReducedMotion() — `app/globals.css`, `components/motion-provider.tsx`, `app/layout.tsx`, `components/character-portrait.tsx` — verified ✅ (new reduced-motion e2e under Playwright emulateMedia: pulse collapses 1.8s→~0, character transform static over 800ms; loop e2e 8/8 still pass)
UX-006 — Added a shared useDialogFocus hook (focus into the sheet on open, trap Tab, close on Escape, restore focus to the opener) and wired it into both bottom sheets with role=dialog/aria-modal/tabIndex=-1 — `lib/hooks/use-dialog-focus.ts`, `components/brainstorm-sheet.tsx`, `components/idea-sheet.tsx` — verified ✅ (new keyboard-only e2e: focus moves in, Tab stays trapped, Escape closes and restores focus to the trigger; ideas e2e 6/6 still pass)
UX-005 — Added installable-app assets via Next.js metadata conventions: web manifest with maskable 192/512 icons, dynamic `next/og` icon/apple-icon/opengraph-image rendering the accent-green "R" brand mark, and wired `manifest`/`appleWebApp`/`openGraph` into root metadata (viewport left untouched) — `app/manifest.ts`, `app/icon.tsx`, `app/apple-icon.tsx`, `app/opengraph-image.tsx`, `app/layout.tsx` — verified ✅ (typecheck clean, eslint clean on all changed files, `next build` compiles and emits `/icon`, `/apple-icon`, `/manifest.webmanifest`, `/opengraph-image` routes)
UX-009 — Enabled adaptive thinking (`thinking: { type: "adaptive" }`, per the claude-api skill) on the brainstorm structuredCall with a bumped 4096 max-tokens budget (thinking shares the budget); added a 30s per-request SDK timeout to structuredCall so a stalled call fails fast into the existing "agent is unavailable" branch. Receipt extraction stays thinking-off — `lib/server/claude.ts`, `lib/server/brainstorm-actions.ts` — verified ✅ (brainstorm e2e passes end-to-end through the mock; typecheck + lint clean)
